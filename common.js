/** 
 * 初始化整数数组
 * @param length 数组长度
 */
function initIntegerArray(length) {
    var integerArray = [];
    for (let i = 1; i <= length; i++) {
        integerArray.push(i);
    }
    return integerArray;
}